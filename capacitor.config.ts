import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.ionisign",
  appName: "IoniSign",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
